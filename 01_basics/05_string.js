const name = "Naman"
const repoCount = 50
    // use `` and ${} to print object refrence in output

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

const gameName = new String(`namanhh`)
    // make sure brackets are *()* are apply after function =>toUpperCase()
console.log(gameName.toUpperCase());
console.log(gameName.charAt(2));
console.log(gameName.indexOf(2));

// indexoff se patat chlega konsa char kis position p hai

